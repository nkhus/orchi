interface:
  display_name: "Orchi"
  short_description: "Develop through branches and GitHub Issues"
  default_prompt: "Use $orchi to continue the work from its GitHub issue and branch."
policy:
  allow_implicit_invocation: true
