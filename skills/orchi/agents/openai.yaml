interface:
  display_name: "Orchi"
  short_description: "Route initiative work by durable state"
  default_prompt: "Use $orchi to continue the initiative from its current controller state."
policy:
  allow_implicit_invocation: true
