---
kind: reference
area: feature
relations:
  addresses: [req-user]
  realizes: [intent/architecture/README.md]
---
# Selected API epic design

Inspect the actual closed values checkpoint before authoring this plan. Expose ANSWER through src/api.py using the verified value contract; keep imports without external effects. Bind the accepted implementation before-images and Working documentation in the task context. The registered API check observes three. No extra service, persistence or deployment boundary is introduced.
