---
kind: requirements
area: feature
requirements: [req-user]
---
# Accepted requirements

<a id="req-user"></a>
## Observable result

The API exposes the combined value three, derived from internal values one and two. Importing these modules has no external effects. The public interface remains a plain Python module. Acceptance checks observe both values and the combined API result.
