name: Orchi Initiative
description: A request decomposed into Epics on an initiative branch.
title: "[TAG] "
labels: [Initiative]
body:
  - type: markdown
    attributes:
      value: "Replace TAG with this Initiative's own tag: 2-8 uppercase letters or digits that no other issue uses, for example [PAY] Card payments."
  - type: textarea
    id: request
    attributes:
      label: Original request
      description: The request as agreed with the requester, kept distinct from later decisions.
    validations:
      required: true
  - type: textarea
    id: outcome
    attributes:
      label: Agreed outcome
      description: Observable result and explicit exclusions.
    validations:
      required: true
  - type: input
    id: plan
    attributes:
      label: Plan
      description: Link to docs/initiatives/<tag>-<slug>/README.md on the initiative/<tag>-<slug> branch.
