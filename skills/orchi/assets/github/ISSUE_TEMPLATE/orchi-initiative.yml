name: Orchi Initiative
description: A request decomposed into Epics on an initiative branch.
title: "Initiative: "
labels: [Initiative]
body:
  - type: textarea
    id: request
    attributes:
      label: Original request
      description: The request as agreed with the requester, kept distinct from later decisions.
    validations:
      required: true
  - type: textarea
    id: outcome
    attributes:
      label: Agreed outcome
      description: Observable result and explicit exclusions.
    validations:
      required: true
  - type: input
    id: plan
    attributes:
      label: Plan
      description: Link to docs/initiatives/<slug>/README.md on the initiative/<slug> branch.
