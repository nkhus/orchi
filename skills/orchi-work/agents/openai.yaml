interface:
  display_name: "Orchi Work"
  short_description: "Execute ready tasks from approved packets"
  default_prompt: "Use $orchi-work to continue the initiative from its current controller state."
policy:
  allow_implicit_invocation: false
