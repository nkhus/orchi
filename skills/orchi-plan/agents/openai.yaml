interface:
  display_name: "Orchi Plan"
  short_description: "Design only the next executable epic"
  default_prompt: "Use $orchi-plan to continue the initiative from its current controller state."
policy:
  allow_implicit_invocation: false
