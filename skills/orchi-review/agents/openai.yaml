interface:
  display_name: "Orchi Review"
  short_description: "Run bounded evidence-based review campaigns"
  default_prompt: "Use $orchi-review to continue the initiative from its current controller state."
policy:
  allow_implicit_invocation: false
