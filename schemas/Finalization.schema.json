{
  "$defs": {
    "KnowledgeEdit": {
      "additionalProperties": false,
      "properties": {
        "target": {
          "minLength": 1,
          "title": "Target",
          "type": "string"
        },
        "action": {
          "enum": [
            "replace",
            "retire",
            "revalidate"
          ],
          "title": "Action",
          "type": "string"
        },
        "content": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Content"
        },
        "artifacts": {
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "minItems": 1,
          "title": "Artifacts",
          "type": "array"
        },
        "checks": {
          "items": {
            "maxLength": 80,
            "pattern": "^[a-z0-9]+(?:[-.][a-z0-9]+)*$",
            "type": "string"
          },
          "minItems": 1,
          "title": "Checks",
          "type": "array"
        },
        "reason": {
          "minLength": 1,
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "target",
        "action",
        "artifacts",
        "checks",
        "reason"
      ],
      "title": "KnowledgeEdit",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "format": {
      "const": "orchi-finalization",
      "default": "orchi-finalization",
      "title": "Format",
      "type": "string"
    },
    "initiative_id": {
      "maxLength": 80,
      "pattern": "^[a-z0-9]+(?:[-.][a-z0-9]+)*$",
      "title": "Initiative Id",
      "type": "string"
    },
    "based_on": {
      "pattern": "^[0-9a-f]{40}$",
      "title": "Based On",
      "type": "string"
    },
    "report": {
      "minLength": 1,
      "title": "Report",
      "type": "string"
    },
    "entries": {
      "items": {
        "$ref": "#/$defs/KnowledgeEdit"
      },
      "title": "Entries",
      "type": "array"
    },
    "acceptance_checks": {
      "minProperties": 1,
      "patternProperties": {
        "^[a-z0-9]+(?:[-.][a-z0-9]+)*$": {
          "items": {
            "maxLength": 80,
            "pattern": "^[a-z0-9]+(?:[-.][a-z0-9]+)*$",
            "type": "string"
          },
          "type": "array"
        }
      },
      "propertyNames": {
        "maxLength": 80
      },
      "title": "Acceptance Checks",
      "type": "object"
    }
  },
  "required": [
    "initiative_id",
    "based_on",
    "report",
    "entries",
    "acceptance_checks"
  ],
  "title": "Finalization",
  "type": "object"
}
